[package]
name = "rinha-db"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.41"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "my_benchmark"
harness = false
